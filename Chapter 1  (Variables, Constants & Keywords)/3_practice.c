#include<stdio.h>
#include<stdlib.h>

int main()
{
    float celcius = 3;
    float far = (celcius * 9 /5) + 32;
    printf("The valur of celcius temperature in farenhiet is:%f ", far);
    return 0;
}